# Use local API (run: cd backend/lambda && npm run start:local — default http://localhost:3000).
# Restart `npm run dev` in customer-web after changing this file.
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000
