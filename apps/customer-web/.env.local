# Default for `npm run dev` in apps/customer-web — talks to deployed dev API + RDS (no local Postgres needed).
NEXT_PUBLIC_API_BASE_URL=https://z0b3obweb6.execute-api.ap-south-1.amazonaws.com
# Full local API + DB only when using: npm run local:stack:customer (needs Postgres + backend/lambda `npm run start:local`)
# NEXT_PUBLIC_API_BASE_URL=http://localhost:3000
