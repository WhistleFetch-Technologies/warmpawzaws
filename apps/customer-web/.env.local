# Customer-web → dev API Gateway (same backend as admin/vendor; same DB as deployed apps).
# Switch to http://localhost:3000 (and start backend with `npm run start:local`) when working against the local Lambda + local Postgres.
# Remote dev API (same as `npm run dev:customer` from repo root). Restart `npm run dev` after edits.
# For local Lambda instead: http://localhost:3000 and run `npm run start:local` in backend/lambda.
NEXT_PUBLIC_API_BASE_URL=https://z0b3obweb6.execute-api.ap-south-1.amazonaws.com
