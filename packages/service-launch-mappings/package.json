{
  "name": "@warmpawz/service-launch-mappings",
  "version": "1.0.0",
  "private": true,
  "description": "Canonical catalog slug → launch service id → customer home screen (Marketing + customer home)",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "node -e \"try{require('fs').rmSync('dist',{recursive:true,force:true})}catch(e){}\""
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "warmpawz-lambda-handlers": "file:../../backend/lambda"
  }
}
