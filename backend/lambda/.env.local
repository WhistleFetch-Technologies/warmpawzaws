# Local Development Environment Variables
# Update these with your actual values if needed

# Database (Local PostgreSQL or Remote)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=warmpawz
DB_USER=postgres
DB_PASSWORD=postgres

# Cognito (Test values - will use fallback in UAT mode)
COGNITO_USER_POOL_ID=ap-south-1_TEST123
COGNITO_CLIENT_ID=test-client-id

# SNS (Mock for local)
SNS_SMS_TOPIC_ARN=arn:aws:sns:ap-south-1:123456789012:test-sms

# Razorpay (Test keys)
RAZORPAY_KEY_ID=test-key-id
RAZORPAY_KEY_SECRET=test-key-secret
RAZORPAY_WEBHOOK_SECRET=test-webhook-secret

# Feature Flags
UAT_MODE=true
