/** Minimum net balance (INR) before vendor can call POST /settlements/request */
export const MIN_VENDOR_PAYOUT_REQUEST_AMOUNT_INR = 5000;

/** Keep in sync with vendor-web payout UI copy */
export const MIN_VENDOR_PAYOUT_REQUEST_ERROR_MESSAGE =
  'Minimum balance to request for a payout is 5000';
